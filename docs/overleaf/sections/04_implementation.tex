\section{Implementation, Technologies, and Challenges}
\label{sec:implementation}

% Target: ~350 words

\subsection{Technologies}
% - Python 3.x, Stable-Baselines3, BeamNGpy, Gymnasium, TensorBoard
% - Flask launcher UI for multi-run management; remote SSH access via scheduled task

\subsection{Key Challenges}

\subsubsection{3D Projection Bug}
% ~80 words
% Hirochi Raceway overpass: two track sections only 0.77 m apart in XY but 6.75 m apart in Z.
% XY-only nearest-point projection assigned wrong segment -> phantom ~998 m progress jumps.
% Fixed by including Z in disambiguation distance (query_utils.py).
% All four conditions A-D trained on corrected projection.

\subsubsection{BeamNG Stability}
% ~60 words
% legacy_60hz crashed after ~40 episodes; det50ms stable for 278k steps.
% All conditions used det50ms after this was identified.

\subsubsection{Deterministic vs Stochastic Evaluation}
% ~60 words
% model.predict(deterministic=True) returns Gaussian policy mean, which can underperform
% stochastic sampling in continuous control near grip limits.
% Best-checkpoint evaluation reported alongside training ep_rew_mean; both metrics are reported.

\subsubsection{Reward Config Contamination}
% ~50 words
% Early run named "v2" used V1 reward weights.
% Documented and excluded; conditions defined by verified run_config.json entries.

